name: huwiya_sdk_example
description: Minimal example app for the Huwiya Flutter SDK.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.11.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  huwiya_sdk:
    path: ..

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
